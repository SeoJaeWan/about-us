import Footer from "@/components/v1/templates/footer/footer";
import Header from "@/components/v1/templates/header/header";
import StyledComponentsRegistry from "@/lib/v1/registry";
import Theme from "@/lib/v1/theme";
import localFont from "next/font/local";

const pretendard = localFont({
  src: "../../../assets/v1/font/PretendardVariable.woff2",
  display: "swap",
  weight: "45 920",
  variable: "--font-pretendard",
});

const concertOne = localFont({
  src: "../../../assets/v1/font/ConcertOne.ttf",
  display: "swap",
  weight: "45 920",
  variable: "--font-concertone",
});

const londrinaSketch = localFont({
  src: "../../../assets/v1/font/LondrinaSketch.ttf",
  display: "swap",
  weight: "45 920",
  variable: "--font-londrinaSketch",
});

export const metadata = {
  title: "서재완 포트폴리오",
  description: "Generated by create next app",
};

export default function RootLayout({ children }) {
  return (
    <html lang="ko">
      <body
        className={`${pretendard.variable} ${concertOne.variable} ${londrinaSketch.variable}`}
      >
        <StyledComponentsRegistry>
          <Theme>
            <Header />
            {children}
            <Footer />
          </Theme>
        </StyledComponentsRegistry>
      </body>
    </html>
  );
}
