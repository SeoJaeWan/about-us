import styled from "styled-components";

const Container = styled.section`
  width: 100%;
  height: 100vh;

  padding: 0 6vw;
  padding-top: 78px;
`;

const SectionStyle = {
  Container,
};

export default SectionStyle;
